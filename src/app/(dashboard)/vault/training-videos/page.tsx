/**
 * /vault/training-videos
 *
 * Canonical URL for the training video library.
 * Re-exports the same component as /vault/videos so both URLs work.
 */
export { default } from "@/app/(dashboard)/vault/videos/page";
